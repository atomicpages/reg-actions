# Keep GitHub Actions SHA pins current. Dependabot resolves each
# `uses:` pin to the latest release tag and opens a PR with the new
# commit SHA (and updates the trailing `# vX.Y.Z` comment).
version: 2
updates:
  - package-ecosystem: github-actions
    directory: /
    schedule:
      interval: weekly
